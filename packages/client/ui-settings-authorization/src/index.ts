/** Host loader entry for the authorization-tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the authorization tab. */
export function apply(): void {}
